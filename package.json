{
  "name": "alta-core-front",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "bun build ./src/index.ts ./src/**/*.ts ./src/**/**/*.ts --outdir=dist --minify-syntax",
    "test": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "@citizenfx/client": "2.0.6878-1",
    "bun-types": "^1.0.6",
    "eslint": "^8.31.0"
  }
}
